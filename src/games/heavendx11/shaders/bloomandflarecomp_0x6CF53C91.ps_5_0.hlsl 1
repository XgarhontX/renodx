// ---- Created with 3Dmigoto v1.3.16 on Thu Mar 05 16:07:11 2026

cbuffer _Globals : register(b0)
{
  float bright_exposure : packoffset(c0);
}

SamplerState s_sampler_0_s : register(s0);
SamplerState s_sampler_1_s : register(s1);
Texture2D<float4> s_texture_0 : register(t0);
Texture2D<float4> s_texture_1 : register(t1);


// 3Dmigoto declarations
#define cmp -


void main(
  float4 v0 : SV_POSITION0,
  float2 v1 : TEXCOORD0,
  out float4 o0 : SV_TARGET0)
{
  float4 r0,r1;
  uint4 bitmask, uiDest;
  float4 fDest;

  r0.x = s_texture_1.Sample(s_sampler_1_s, float2(0.5,0.5)).x;
  r0.x = r0.x * r0.x;
  r0.x = bright_exposure / r0.x;
  r0.x = min(1, r0.x);
  r1.xyzw = s_texture_0.Sample(s_sampler_0_s, v1.xy).xyzw;
  o0.xyzw = r1.xyzw * r0.xxxx;
  return;
}