// ---- Created with 3Dmigoto v1.3.16 on Sat Sep 13 00:34:16 2025
#include "../shared.h"

Texture2D<float4> t0 : register(t0);

SamplerState s0_s : register(s0);

cbuffer cb2 : register(b2)
{
  float4 cb2[7];
}




// 3Dmigoto declarations
#define cmp -


void main(
  float4 v0 : SV_POSITION0,
  float4 v1 : COLOR0,
  float2 v2 : TEXCOORD0,
  out float4 o0 : SV_TARGET0)
{
  float4 r0;
  uint4 bitmask, uiDest;
  float4 fDest;
  if (!CUSTOM_IS_UI) discard;
  r0.xyzw = t0.SampleBias(s0_s, v2.xy, cb2[6].y).xyzw;
  r0.xyzw = v1.xyzw * r0.xyzw;
  o0.xyzw = min(float4(32255,32255,32255,32255), r0.xyzw);
  return;
}